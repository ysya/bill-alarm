services:
  bill-alarm:
    image: ghcr.io/ysya/bill-alarm:latest
    ports:
      - "${PORT:-3100}:80"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
